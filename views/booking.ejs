<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book a Turf</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@1.8.0/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="container mx-auto px-4 py-8">
        <h2 class="text-xl mb-4">Book a Turf</h2>
        <form action="/booking" method="POST">
            <label for="ground" class="block mb-2">Select Ground:</label>
            <select name="ground" id="ground" class="border p-2 mb-4 w-full" required>
                <% grounds.forEach(ground => { %>
                    <option value="<%= ground._id %>"><%= ground.name %></option>
                <% }) %>
            </select>

            <label for="timeSlot" class="block mb-2">Select Time Slot:</label>
            <input type="datetime-local" name="timeSlot" class="border p-2 mb-4 w-full" required>

            <button type="submit" class="bg-green-500 text-white px-4 py-2 rounded">Book Now</button>
        </form>
        <a href="/" class="text-blue-500">Back to Home</a>
    </div>
</body>
</html>
